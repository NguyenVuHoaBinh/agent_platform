package viettel.dac.toolserviceregistry.model.enums;

/**
 * Enum representing authentication types for API calls.
 */
public enum AuthenticationType {
    NONE,
    API_KEY,
    BASIC,
    BEARER_TOKEN,
    OAUTH2,
    CUSTOM
}