package viettel.dac.toolserviceregistry.model.enums;

/**
 * Enum representing the types of tools in the registry.
 */
public enum ToolType {
    API_TOOL,       // Tools that interact with external APIs
    OTHER          // For other types of tools (placeholder for future expansion)
}